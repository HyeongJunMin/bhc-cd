{
  "name": "bhc-c",
  "version": "0.1.0",
  "private": true,
  "description": "Web 3-cushion billiards monorepo",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint"
  }
}
